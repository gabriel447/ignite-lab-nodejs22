paths = configuração no tsconfig para usar caminhos como se fosse variáveis
mappers = converter/transformar atributos de uma entidade para usa-los de formas diferentes em vários níveis da aplicação
npx tsc --noEmit
npm run test:watch
factory patterns = uma função que retorna um objeto, usada para abstrair criação de objetos que são repetidos e são usados em vários lugares da aplicação
* como se fosse uma fabrica, ela produz uma notificação por exemplo